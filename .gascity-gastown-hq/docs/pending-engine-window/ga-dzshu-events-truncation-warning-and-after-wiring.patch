diff --git a/cmd/gc/cmd_events.go b/cmd/gc/cmd_events.go
index a7f3d4533..1e359ca3f 100644
--- a/cmd/gc/cmd_events.go
+++ b/cmd/gc/cmd_events.go
@@ -186,7 +186,7 @@ DTO or SSE envelope.`,
 				}
 				return nil
 			}
-			if cmdEvents(apiURL, typeFilter, sinceFlag, payloadMatch, stdout, stderr) != 0 {
+			if cmdEvents(apiURL, typeFilter, sinceFlag, payloadMatch, afterFlag, afterCursor, stdout, stderr) != 0 {
 				return errExit
 			}
 			return nil
@@ -233,7 +233,7 @@ Output is one JSON line. Empty active logs are successful no-ops.`,
 	return cmd
 }
 
-func cmdEvents(apiURLOverride, typeFilter, sinceFlag string, payloadMatchArgs []string, stdout, stderr io.Writer) int {
+func cmdEvents(apiURLOverride, typeFilter, sinceFlag string, payloadMatchArgs []string, afterSeq uint64, afterCursor string, stdout, stderr io.Writer) int {
 	if err := validateEventsSince(sinceFlag); err != nil {
 		fmt.Fprintf(stderr, "gc events: %v\n", err) //nolint:errcheck
 		return 1
@@ -247,7 +247,11 @@ func cmdEvents(apiURLOverride, typeFilter, sinceFlag string, payloadMatchArgs []
 	if code != 0 {
 		return code
 	}
-	return doEvents(scope, typeFilter, sinceFlag, pm, stdout, stderr)
+	if err := validateEventsCursor(scope, afterSeq, afterCursor); err != nil {
+		fmt.Fprintf(stderr, "gc events: %v\n", err) //nolint:errcheck
+		return 1
+	}
+	return doEvents(scope, typeFilter, sinceFlag, pm, afterSeq, afterCursor, stdout, stderr)
 }
 
 func cmdEventsSeq(apiURLOverride string, stdout, stderr io.Writer) int {
@@ -485,14 +489,14 @@ func validateEventsSince(sinceFlag string) error {
 	return nil
 }
 
-func doEvents(scope eventsAPIScope, typeFilter, sinceFlag string, payloadMatch map[string][]string, stdout, stderr io.Writer) int {
+func doEvents(scope eventsAPIScope, typeFilter, sinceFlag string, payloadMatch map[string][]string, afterSeq uint64, afterCursor string, stdout, stderr io.Writer) int {
 	if scope.localOnly {
 		fallback, _, fallbackErr := readLocalCityEvents(scope, stoppedCityLocalFallbackError(scope), typeFilter, sinceFlag, stderr)
 		if fallbackErr != nil {
 			fmt.Fprintf(stderr, "gc events: %v\n", fallbackErr) //nolint:errcheck
 			return 1
 		}
-		fallback = filterCityEvents(fallback, 0, typeFilter, payloadMatch)
+		fallback = filterCityEvents(fallback, afterSeq, typeFilter, payloadMatch)
 		return printJSONLines(fallback, stdout, stderr)
 	}
 
@@ -511,24 +515,31 @@ func doEvents(scope eventsAPIScope, typeFilter, sinceFlag string, payloadMatch m
 			fmt.Fprintf(stderr, "gc events: %v\n", err) //nolint:errcheck
 			return 1
 		}
+		if strings.TrimSpace(afterCursor) != "" {
+			matches := filterSupervisorEventsAfterCursor(items, afterCursor, typeFilter, payloadMatch)
+			return printJSONLines(matches, stdout, stderr)
+		}
 		items = filterSupervisorEvents(items, typeFilter, payloadMatch)
 		return printJSONLines(items, stdout, stderr)
 	}
 
-	items, err := fetchCityEvents(ctx, client, scope.cityName, typeFilter, sinceFlag)
+	items, truncated, err := fetchCityEvents(ctx, client, scope.cityName, typeFilter, sinceFlag)
 	if err != nil {
 		if fallback, ok, fallbackErr := readLocalCityEvents(scope, err, typeFilter, sinceFlag, stderr); ok {
 			if fallbackErr != nil {
 				fmt.Fprintf(stderr, "gc events: %v\n", fallbackErr) //nolint:errcheck
 				return 1
 			}
-			fallback = filterCityEvents(fallback, 0, typeFilter, payloadMatch)
+			fallback = filterCityEvents(fallback, afterSeq, typeFilter, payloadMatch)
 			return printJSONLines(fallback, stdout, stderr)
 		}
 		fmt.Fprintf(stderr, "gc events: %v\n", err) //nolint:errcheck
 		return 1
 	}
-	items = filterCityEvents(items, 0, typeFilter, payloadMatch)
+	if truncated {
+		fmt.Fprintln(stderr, "gc events: showing the newest 500 matching events; more history exists beyond this page. Narrow --since/--type, or read .gc/events.jsonl (+ .gz archives) directly for the full history.") //nolint:errcheck
+	}
+	items = filterCityEvents(items, afterSeq, typeFilter, payloadMatch)
 	return printJSONLines(items, stdout, stderr)
 }
 
@@ -814,7 +825,14 @@ func doEventsWatch(scope eventsAPIScope, typeFilter string, payloadMatch map[str
 
 	resumeSeq := afterSeq
 	if resumeSeq > 0 {
-		items, err := fetchCityEvents(ctx, client, scope.cityName, "", "")
+		// truncated is intentionally ignored here: this is a fast-path probe
+		// of recent history, not the final answer. A miss within the capped
+		// page (full backlog >500) falls through to streamCityEvents below,
+		// which catches up from resumeSeq via the SSE stream's own forward
+		// replay — not subject to this page cap — so completeness still
+		// holds; warning here would just be page-cap noise on every --watch
+		// call against a busy city.
+		items, _, err := fetchCityEvents(ctx, client, scope.cityName, "", "")
 		if err != nil {
 			if shouldUseLocalCityEventsFallback(scope, err) {
 				printStreamingCityAPIRequirement("--watch", stderr)
@@ -954,10 +972,24 @@ func probeCityEventsReachable(ctx context.Context, client *genclient.ClientWithR
 // fetchCityEvents fetches the newest page of city events (up to 500). It
 // deliberately does NOT follow next_cursor: gc events means "recent
 // activity", and a full descending drain of a large city's event history
-// (100 MB+ logs) would blow the command timeout for no user benefit. The
-// API serves the page seq-DESC (newest first); gc events prints
+// (100 MB+ logs) would blow the command timeout for no user benefit. This
+// is upstream's own red-teamed call (gastownhall/gascity#4194, finding
+// "Major — CLI full-history drain"), pinned by
+// TestFetchCityEventsSinglePageChronological — do not restore the loop.
+//
+// The API serves the page seq-DESC (newest first); gc events prints
 // chronologically, so the page is re-sorted ascending.
-func fetchCityEvents(ctx context.Context, client *genclient.ClientWithResponses, cityName, typeFilter, sinceFlag string) ([]cliWireEvent, error) {
+//
+// truncated reports whether the server minted a next_cursor, i.e. whether
+// more matching history exists beyond this page. Before ga-dzshu this was
+// silently dropped: a --since/--type/--after query could be capped at 500
+// with exit 0 and no signal, which produced wrong answers for forensic
+// reads (a 24h join concluded 13 of 14 mails never closed; the on-disk log
+// showed 12 of 14 closed). The cap itself is intentional; only the silence
+// was the bug. Callers that print results to the user should warn on
+// truncated=true; internal fast-path probes (doEventsWatch) may ignore it
+// since their fallback path is complete regardless.
+func fetchCityEvents(ctx context.Context, client *genclient.ClientWithResponses, cityName, typeFilter, sinceFlag string) (items []cliWireEvent, truncated bool, err error) {
 	limit := int64(500)
 	params := &genclient.GetV0CityByCityNameEventsParams{
 		Limit: &limit,
@@ -970,24 +1002,25 @@ func fetchCityEvents(ctx context.Context, client *genclient.ClientWithResponses,
 	}
 	resp, err := client.GetV0CityByCityNameEventsWithResponse(ctx, cityName, params)
 	if err != nil {
-		return nil, &eventsAPITransportError{err: err}
+		return nil, false, &eventsAPITransportError{err: err}
 	}
 	if err := eventsListError(resp.StatusCode(), resp.ApplicationproblemJSONDefault); err != nil {
-		return nil, err
+		return nil, false, err
 	}
 	if resp.JSON200 == nil || resp.JSON200.Items == nil {
-		return nil, nil
+		return nil, false, nil
 	}
+	truncated = resp.JSON200.NextCursor != nil && strings.TrimSpace(*resp.JSON200.NextCursor) != ""
 	all := make([]cliWireEvent, 0, len(*resp.JSON200.Items))
 	for _, item := range *resp.JSON200.Items {
 		wire, err := cityWireEventFromTyped(item)
 		if err != nil {
-			return nil, fmt.Errorf("decoding city event list item: %w", err)
+			return nil, false, fmt.Errorf("decoding city event list item: %w", err)
 		}
 		all = append(all, wire)
 	}
 	sort.Slice(all, func(i, j int) bool { return all[i].Seq < all[j].Seq })
-	return all, nil
+	return all, truncated, nil
 }
 
 func fetchCityHeadIndex(ctx context.Context, client *genclient.ClientWithResponses, cityName string) (string, error) {
diff --git a/cmd/gc/cmd_events_test.go b/cmd/gc/cmd_events_test.go
index d1196545e..6aff2d68d 100644
--- a/cmd/gc/cmd_events_test.go
+++ b/cmd/gc/cmd_events_test.go
@@ -75,7 +75,7 @@ func TestDoEventsCityDefaultUsesJSONLItems(t *testing.T) {
 	defer server.Close()
 
 	var stdout, stderr bytes.Buffer
-	code := doEvents(eventsAPIScope{apiURL: server.URL, cityName: "mc-city"}, "", "", nil, &stdout, &stderr)
+	code := doEvents(eventsAPIScope{apiURL: server.URL, cityName: "mc-city"}, "", "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -109,7 +109,7 @@ func TestDoEventsSupervisorDefaultUsesTaggedJSONLItems(t *testing.T) {
 	defer server.Close()
 
 	var stdout, stderr bytes.Buffer
-	code := doEvents(eventsAPIScope{apiURL: server.URL}, "", "", nil, &stdout, &stderr)
+	code := doEvents(eventsAPIScope{apiURL: server.URL}, "", "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -222,7 +222,7 @@ func TestDoEventsFallsBackToLocalCityEventsWhenCityStopped(t *testing.T) {
 		apiURL:   server.URL,
 		cityName: "mc-city",
 		cityPath: cityDir,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -262,7 +262,7 @@ func TestDoEventsFallsBackToLocalCityEventsOnTypedStoppedCityNotFound(t *testing
 		apiURL:   server.URL,
 		cityName: "mc-city",
 		cityPath: cityDir,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -302,7 +302,7 @@ func TestDoEventsDoesNotFallbackToLocalCityEventsForGeneric404(t *testing.T) {
 		apiURL:   server.URL,
 		cityName: "mc-city",
 		cityPath: cityDir,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 1 {
 		t.Fatalf("doEvents = %d, want 1; stdout=%s stderr=%s", code, stdout.String(), stderr.String())
 	}
@@ -341,7 +341,7 @@ func TestDoEventsDoesNotFallbackToLocalCityEventsForExplicitAPI(t *testing.T) {
 		cityName:    "mc-city",
 		cityPath:    cityDir,
 		explicitAPI: true,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 1 {
 		t.Fatalf("doEvents = %d, want 1; stdout=%s stderr=%s", code, stdout.String(), stderr.String())
 	}
@@ -381,7 +381,7 @@ func TestDoEventsFallsBackToLocalCityEventsForExplicitLocalSupervisorAPI(t *test
 		cityPath:           cityDir,
 		explicitAPI:        true,
 		localSupervisorAPI: true,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -416,7 +416,7 @@ func TestDoEventsFallsBackToLocalCityEventsForExplicitLocalSupervisorAPITranspor
 		cityPath:           cityDir,
 		explicitAPI:        true,
 		localSupervisorAPI: true,
-	}, events.SessionStopped, "", nil, &stdout, &stderr)
+	}, events.SessionStopped, "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -458,7 +458,7 @@ func TestDoEventsReadsCustomCityEventTypesThroughAPI(t *testing.T) {
 		apiURL:   server.URL,
 		cityName: "mc-city",
 		cityPath: cityDir,
-	}, "app.custom", "", nil, &stdout, &stderr)
+	}, "app.custom", "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -494,7 +494,7 @@ func TestDoEventsDoesNotReadLocalUntypedCityEventsForExplicitRemoteAPI(t *testin
 		cityName:    "mc-city",
 		cityPath:    cityDir,
 		explicitAPI: true,
-	}, "app.custom", "", nil, &stdout, &stderr)
+	}, "app.custom", "", nil, 0, "", &stdout, &stderr)
 	if code != 0 {
 		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
 	}
@@ -997,7 +997,7 @@ func TestParsePayloadMatch(t *testing.T) {
 
 func TestCmdEventsValidatesLocalFlagsBeforeAPIDiscovery(t *testing.T) {
 	var stdout, stderr bytes.Buffer
-	code := cmdEvents("", "", "notaduration", nil, &stdout, &stderr)
+	code := cmdEvents("", "", "notaduration", nil, 0, "", &stdout, &stderr)
 	if code == 0 {
 		t.Fatalf("cmdEvents invalid since = 0, want non-zero")
 	}
@@ -1364,7 +1364,7 @@ func TestFetchCityEventsSinglePageChronological(t *testing.T) {
 	if err != nil {
 		t.Fatalf("client: %v", err)
 	}
-	got, err := fetchCityEvents(context.Background(), client, "mc-city", "", "")
+	got, truncated, err := fetchCityEvents(context.Background(), client, "mc-city", "", "")
 	if err != nil {
 		t.Fatalf("fetchCityEvents: %v", err)
 	}
@@ -1376,4 +1376,189 @@ func TestFetchCityEventsSinglePageChronological(t *testing.T) {
 			t.Fatalf("event[%d].Seq = %d, want %d (chronological ascending)", i, item.Seq, i+4)
 		}
 	}
+	if !truncated {
+		t.Fatalf("truncated = false, want true: the response minted a next_cursor, so more history exists beyond this page")
+	}
+}
+
+// TestFetchCityEventsReportsNotTruncatedWhenPageIsComplete is the negative
+// case for the truncated signal: when the server's page has no next_cursor
+// (every matching event fit), fetchCityEvents must not claim more history
+// exists — ga-dzshu's fix adds this signal, it must not over-fire.
+func TestFetchCityEventsReportsNotTruncatedWhenPageIsComplete(t *testing.T) {
+	page := []cliWireEvent{
+		{Actor: "gc", Seq: 2, Type: "e.t", Ts: time.Unix(1700000020, 0).UTC()},
+		{Actor: "gc", Seq: 1, Type: "e.t", Ts: time.Unix(1700000010, 0).UTC()},
+	}
+	server := newEventsTestServer(t, testEventRoutes{
+		cityEvents: func(w http.ResponseWriter, _ *http.Request) {
+			w.Header().Set("X-GC-Index", "2")
+			writeJSONResponse(t, w, cityEventsListResponse(t, page))
+		},
+	})
+	defer server.Close()
+
+	client, err := genclient.NewClientWithResponses(server.URL)
+	if err != nil {
+		t.Fatalf("client: %v", err)
+	}
+	got, truncated, err := fetchCityEvents(context.Background(), client, "mc-city", "", "")
+	if err != nil {
+		t.Fatalf("fetchCityEvents: %v", err)
+	}
+	if len(got) != 2 {
+		t.Fatalf("got %d events, want 2", len(got))
+	}
+	if truncated {
+		t.Fatalf("truncated = true, want false: the response carried no next_cursor")
+	}
+}
+
+// TestDoEventsCityWarnsOnTruncatedPage pins ga-dzshu's actual fix: when the
+// city events page is truncated (next_cursor present), doEvents must warn on
+// stderr instead of silently returning a partial result with exit 0. Before
+// this fix, a --since/--type query capped at 500 produced no signal at all
+// (measured: a 24h bead.closed join concluded 13 of 14 mails never closed
+// when the on-disk log showed 12 of 14 closed).
+func TestDoEventsCityWarnsOnTruncatedPage(t *testing.T) {
+	items := []cliWireEvent{
+		{Actor: "gc", Seq: 2, Type: "e.t", Ts: time.Unix(1700000020, 0).UTC()},
+		{Actor: "gc", Seq: 1, Type: "e.t", Ts: time.Unix(1700000010, 0).UTC()},
+	}
+	server := newEventsTestServer(t, testEventRoutes{
+		cityEvents: func(w http.ResponseWriter, _ *http.Request) {
+			w.Header().Set("X-GC-Index", "2")
+			body := cityEventsListResponse(t, items)
+			next := "v1:eyJrIjoic3EiLCJzIjoxfQ"
+			body.NextCursor = &next
+			writeJSONResponse(t, w, body)
+		},
+	})
+	defer server.Close()
+
+	var stdout, stderr bytes.Buffer
+	code := doEvents(eventsAPIScope{apiURL: server.URL, cityName: "mc-city"}, "", "", nil, 0, "", &stdout, &stderr)
+	if code != 0 {
+		t.Fatalf("doEvents = %d, want 0 (truncation is a warning, not a failure); stderr=%s", code, stderr.String())
+	}
+	if !strings.Contains(stderr.String(), "truncat") && !strings.Contains(stderr.String(), "more history") {
+		t.Fatalf("stderr = %q, want a truncation warning", stderr.String())
+	}
+	lines := strings.Split(strings.TrimSpace(stdout.String()), "\n")
+	if len(lines) != 2 {
+		t.Fatalf("got %d JSONL lines on stdout, want 2 (the capped page's items, unchanged)", len(lines))
+	}
+}
+
+// TestDoEventsCityNoWarningWhenPageComplete guards against over-firing the
+// new truncation warning: a normal, complete page must print nothing extra
+// on stderr.
+func TestDoEventsCityNoWarningWhenPageComplete(t *testing.T) {
+	items := []cliWireEvent{
+		{Actor: "gc", Seq: 1, Type: "e.t", Ts: time.Unix(1700000010, 0).UTC()},
+	}
+	server := newEventsTestServer(t, testEventRoutes{
+		cityEvents: func(w http.ResponseWriter, _ *http.Request) {
+			w.Header().Set("X-GC-Index", "1")
+			writeJSONResponse(t, w, cityEventsListResponse(t, items))
+		},
+	})
+	defer server.Close()
+
+	var stdout, stderr bytes.Buffer
+	code := doEvents(eventsAPIScope{apiURL: server.URL, cityName: "mc-city"}, "", "", nil, 0, "", &stdout, &stderr)
+	if code != 0 {
+		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
+	}
+	if stderr.Len() > 0 {
+		t.Fatalf("stderr = %q, want empty (page was complete, no next_cursor)", stderr.String())
+	}
+}
+
+// TestDoEventsCityAppliesAfterSeq pins the second ga-dzshu defect: --after
+// was parsed but never threaded into the plain-list path, so it silently had
+// no effect outside --follow/--watch. doEvents must now use it to resume
+// past a known seq via the existing filterCityEvents(afterSeq) parameter.
+func TestDoEventsCityAppliesAfterSeq(t *testing.T) {
+	items := []cliWireEvent{
+		{Actor: "gc", Seq: 1, Type: "e.t", Ts: time.Unix(1700000010, 0).UTC()},
+		{Actor: "gc", Seq: 2, Type: "e.t", Ts: time.Unix(1700000020, 0).UTC()},
+		{Actor: "gc", Seq: 3, Type: "e.t", Ts: time.Unix(1700000030, 0).UTC()},
+	}
+	server := newEventsTestServer(t, testEventRoutes{
+		cityEvents: func(w http.ResponseWriter, _ *http.Request) {
+			w.Header().Set("X-GC-Index", "3")
+			writeJSONResponse(t, w, cityEventsListResponse(t, items))
+		},
+	})
+	defer server.Close()
+
+	var stdout, stderr bytes.Buffer
+	code := doEvents(eventsAPIScope{apiURL: server.URL, cityName: "mc-city"}, "", "", nil, 1, "", &stdout, &stderr)
+	if code != 0 {
+		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
+	}
+	lines := strings.Split(strings.TrimSpace(stdout.String()), "\n")
+	if len(lines) != 2 {
+		t.Fatalf("got %d JSONL lines, want 2 (seq 2 and 3, resumed after seq 1); output=%q", len(lines), stdout.String())
+	}
+	for _, line := range lines {
+		var item cliWireEvent
+		if err := json.Unmarshal([]byte(line), &item); err != nil {
+			t.Fatalf("unmarshal line: %v; line=%q", err, line)
+		}
+		if item.Seq <= 1 {
+			t.Fatalf("event with Seq=%d leaked through --after 1", item.Seq)
+		}
+	}
+}
+
+// TestDoEventsSupervisorAppliesAfterCursor is the supervisor-scope sibling of
+// TestDoEventsCityAppliesAfterSeq: --after-cursor had the identical gap in
+// list mode, fixable with the pre-existing filterSupervisorEventsAfterCursor
+// helper that --watch already used.
+func TestDoEventsSupervisorAppliesAfterCursor(t *testing.T) {
+	items := []cliWireTaggedEvent{
+		{Actor: "human", City: "alpha", Seq: 3, Ts: time.Unix(1700000000, 0).UTC(), Type: "bead.created"},
+		{Actor: "human", City: "alpha", Seq: 4, Ts: time.Unix(1700000010, 0).UTC(), Type: "bead.created"},
+	}
+	server := newEventsTestServer(t, testEventRoutes{
+		supervisorEvents: func(w http.ResponseWriter, _ *http.Request) {
+			writeJSONResponse(t, w, supervisorEventsListResponse(t, items))
+		},
+	})
+	defer server.Close()
+
+	var stdout, stderr bytes.Buffer
+	code := doEvents(eventsAPIScope{apiURL: server.URL}, "", "", nil, 0, "alpha:3", &stdout, &stderr)
+	if code != 0 {
+		t.Fatalf("doEvents = %d, want 0; stderr=%s", code, stderr.String())
+	}
+	var got cliWireTaggedEvent
+	if err := json.Unmarshal(bytes.TrimSpace(stdout.Bytes()), &got); err != nil {
+		t.Fatalf("unmarshal stdout: %v; output=%s", err, stdout.String())
+	}
+	if got.Seq != 4 {
+		t.Fatalf("got seq=%d, want only seq=4 (resumed after alpha:3)", got.Seq)
+	}
+}
+
+// TestCmdEventsRejectsAfterInSupervisorScope pins the new scope guard added
+// to cmdEvents: --after only makes sense with a city in scope, exactly like
+// the pre-existing check on --follow/--watch (validateEventsCursor). Before
+// this fix cmdEvents never even received afterSeq, so no such guard existed.
+func TestCmdEventsRejectsAfterInSupervisorScope(t *testing.T) {
+	t.Chdir(t.TempDir())
+
+	server := newEventsTestServer(t, testEventRoutes{})
+	defer server.Close()
+
+	var stdout, stderr bytes.Buffer
+	code := cmdEvents(server.URL, "", "", nil, 42, "", &stdout, &stderr)
+	if code == 0 {
+		t.Fatalf("cmdEvents --after in supervisor scope = 0, want non-zero")
+	}
+	if !strings.Contains(stderr.String(), "--after") {
+		t.Fatalf("stderr = %q, want a message naming --after", stderr.String())
+	}
 }
