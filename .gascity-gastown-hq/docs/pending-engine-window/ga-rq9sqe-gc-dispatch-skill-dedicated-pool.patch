diff --git a/internal/bootstrap/gc_dispatch_skill_test.go b/internal/bootstrap/gc_dispatch_skill_test.go
new file mode 100644
index 000000000..12bf3bca2
--- /dev/null
+++ b/internal/bootstrap/gc_dispatch_skill_test.go
@@ -0,0 +1,77 @@
+package bootstrap
+
+import (
+	"io/fs"
+	"strings"
+	"testing"
+)
+
+// gcDispatchSkillPath is the embedded path to the core pack's dispatch
+// doctrine skill, materialized into every role's skill directory (Mayor,
+// crew, dog, refinery, witness, ...) in every rig via .gc/system/packs.
+const gcDispatchSkillPath = "packs/core/skills/gc-dispatch/SKILL.md"
+
+// TestGCDispatchSkillWarnsAboutDedicatedPools is the regression fence for
+// ga-rq9sqe (follow-up to ga-68cijh). The skill taught "<rig>/polecat" as
+// the dispatch pattern in multiple examples with no mention that a rig
+// running its own dedicated pool (e.g. wa-worker, ps-worker) instead of
+// the generic polecat role must not be routed there: the pattern still
+// resolves (gc sling accepts it) but routes to nothing spawned to claim
+// it, and the bead sits routed-but-dead until a watchdog notices. Any
+// role composing a sling command by reading this skill — not just the
+// Mayor — could reproduce the exact dead-route bug ga-68cijh fixed in the
+// Mayor's own prompt template.
+func TestGCDispatchSkillWarnsAboutDedicatedPools(t *testing.T) {
+	data, err := fs.ReadFile(bootstrapAssets, gcDispatchSkillPath)
+	if err != nil {
+		t.Fatalf("read %s: %v", gcDispatchSkillPath, err)
+	}
+	body := string(data)
+
+	t.Run("documents_dedicated_pool_exception", func(t *testing.T) {
+		if !strings.Contains(body, "dedicated pool") {
+			t.Errorf("%s does not mention the dedicated-pool exception "+
+				"(e.g. wa-worker, ps-worker) anywhere. A rig running its "+
+				"own named pool instead of the generic polecat role must "+
+				"not be routed via the generic <rig>/polecat pattern. "+
+				"See ga-68cijh, ga-rq9sqe.", gcDispatchSkillPath)
+		}
+	})
+
+	t.Run("tells_reader_to_check_actual_pool", func(t *testing.T) {
+		if !strings.Contains(body, "rig's actual pool") {
+			t.Errorf("%s does not tell the reader to check the rig's "+
+				"actual pool (e.g. via `gc session list`) before "+
+				"defaulting to the generic <rig>/polecat pattern.",
+				gcDispatchSkillPath)
+		}
+	})
+
+	t.Run("frontend_polecat_example_has_caveat", func(t *testing.T) {
+		idx := strings.Index(body, "frontend/polecat fe-xxx")
+		if idx == -1 {
+			t.Fatalf("%s no longer contains the `frontend/polecat fe-xxx` "+
+				"example this test targets; update the test to match the "+
+				"current example.", gcDispatchSkillPath)
+		}
+		if !strings.Contains(body[idx:], "generic polecat role") {
+			t.Errorf("%s's frontend/polecat example carries no caveat "+
+				"about the generic-polecat-role assumption anywhere after "+
+				"it in the file.", gcDispatchSkillPath)
+		}
+	})
+
+	t.Run("review_target_example_has_caveat", func(t *testing.T) {
+		idx := strings.Index(body, "review_target=<rig>/polecat")
+		if idx == -1 {
+			t.Fatalf("%s no longer contains the `review_target=<rig>/polecat` "+
+				"example this test targets; update the test to match the "+
+				"current example.", gcDispatchSkillPath)
+		}
+		if !strings.Contains(body[idx:], "real work-review target") {
+			t.Errorf("%s's review_target example carries no caveat "+
+				"clarifying it must be the rig's real work-review target, "+
+				"not a blind <rig>/polecat guess.", gcDispatchSkillPath)
+		}
+	})
+}
diff --git a/internal/bootstrap/packs/core/skills/gc-dispatch/SKILL.md b/internal/bootstrap/packs/core/skills/gc-dispatch/SKILL.md
index 2b810ad52..39442cad5 100644
--- a/internal/bootstrap/packs/core/skills/gc-dispatch/SKILL.md
+++ b/internal/bootstrap/packs/core/skills/gc-dispatch/SKILL.md
@@ -29,6 +29,17 @@ bead's rig prefix. The rig's `default_sling_target` in city.toml determines
 where work goes. Example: bead `hw-42` → rig `hello-world` → target
 `hello-world/polecat`.
 
+**Rigs with a dedicated pool:** `<rig>/polecat` is the generic default
+target, not a universal one. A rig that runs its own named pool (e.g.
+`wa-worker`, `ps-worker`) instead of the generic polecat role must be
+routed there directly — the plain `<rig>/polecat` pattern still *resolves*
+(`gc sling` accepts it; a pack-qualified generic role is importable by any
+rig) but if nothing is spawned to listen on that exact identity for this
+rig, the bead sits routed-but-dead until a watchdog notices. Before reusing
+the generic pattern from an example below, check `gc session list` (or `gc
+agent list`) for the rig's actual pool, or the rig's `default_sling_target`
+in city.toml if one is set (ga-68cijh, ga-rq9sqe).
+
 **Rig-scoped beads:** `gc sling` automatically resolves the rig directory
 for rig-scoped bead IDs (e.g. `hw-abc`) and runs `bd update` from there,
 so the rig's `.beads` database is found without manual intervention.
@@ -47,6 +58,10 @@ gc sling frontend/polecat fe-xxx            # Works — bead is in the right db
 If the bead is in the wrong database (e.g. `gc-xxx` in HQ but targeting
 a frontend agent), sling's cross-rig guard will block the route.
 
+`frontend/polecat` above assumes `frontend` uses the generic polecat role.
+Confirm with `gc session list` before reusing this pattern on a rig that
+runs its own dedicated pool (see "Rigs with a dedicated pool" above).
+
 ## Direct dispatch (bead to session config)
 
 ```
@@ -138,6 +153,12 @@ and mail. Best run from a crew worker in the target rig.
 gc sling <coordinator-agent> -f mol-idea-to-plan --var problem="..." --var review_target=<rig>/polecat
 ```
 
+`review_target` must be the rig's real work-review target —
+`<rig>/polecat` only if that rig uses the generic polecat role. On a rig
+with its own dedicated pool, check `gc session list` (or the rig's
+`default_sling_target`) first; the generic pattern resolves but routes to
+nothing if unclaimed (see "Rigs with a dedicated pool" above).
+
 **mol-review-leg** — Helper formula used by `mol-idea-to-plan` review tasks.
 Persists the full report to bead notes, mails the coordinator, closes the bead,
 and drains the session. Usually not slung by hand.
