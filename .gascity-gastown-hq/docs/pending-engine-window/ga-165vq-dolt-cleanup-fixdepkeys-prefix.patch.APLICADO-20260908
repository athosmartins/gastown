diff --git a/cmd/gc/dolt_cleanup_drop_planner.go b/cmd/gc/dolt_cleanup_drop_planner.go
index 53a2ea6e3..d801e9586 100644
--- a/cmd/gc/dolt_cleanup_drop_planner.go
+++ b/cmd/gc/dolt_cleanup_drop_planner.go
@@ -17,8 +17,13 @@ import "strings"
 //   - beads_vr*: orchestrator mail/router_test.go random prefixes
 //   - beads_t[0-9a-f]*: protocol test random prefixes (t + 8 hex chars)
 //   - beads_test_bench_*: benchmark test fixture databases
+//   - fixdepkeys_*: doctor/fix dep-keys test random prefixes (ga-165vq —
+//     third_party/beads/cmd/bd/doctor/fix/dep_keys_test.go creates these
+//     directly against whatever Dolt server BEADS_DOLT_PORT resolves to,
+//     production in this city; they never age out on their own because
+//     nothing else ever writes to them)
 var defaultStaleDatabasePrefixes = []string{
-	"testdb_", "test_guard_", "test_federation_", "doctest_", "doctortest_", "beads_pt", "beads_vr", "beads_t", "beads_test_bench_",
+	"testdb_", "test_guard_", "test_federation_", "doctest_", "doctortest_", "beads_pt", "beads_vr", "beads_t", "beads_test_bench_", "fixdepkeys_",
 }
 
 // systemDatabaseNames are the Dolt/MySQL system databases that SHOW
diff --git a/cmd/gc/dolt_cleanup_drop_planner_test.go b/cmd/gc/dolt_cleanup_drop_planner_test.go
index 5ff98b702..88bec1e40 100644
--- a/cmd/gc/dolt_cleanup_drop_planner_test.go
+++ b/cmd/gc/dolt_cleanup_drop_planner_test.go
@@ -117,6 +117,38 @@ func TestPlanDoltDrops_DefaultPrefixesIncludeGuardAndFederationTests(t *testing.
 	}
 }
 
+func TestPlanDoltDrops_DefaultPrefixesIncludeFixDepKeysTests(t *testing.T) {
+	// ga-165vq: third_party/beads/cmd/bd/doctor/fix/dep_keys_test.go
+	// (TestDependencyKeys_RekeysAndRemovesLeftovers) creates a database named
+	// "fixdepkeys_" + 12 random hex chars directly against whatever Dolt
+	// server BEADS_DOLT_PORT resolves to — production, in this city. The
+	// prefix must be recognized as stale so gc dolt-cleanup actually drops
+	// these instead of treating them as unknown/production forever (they
+	// never age out on their own: nothing else ever writes to them, so their
+	// backup-freshness only ever gets older).
+	all := []string{
+		"hq",
+		"gascity",
+		"fixdepkeys_0d16f874c5d7",
+		"testdb_old",
+	}
+	protected := []string{"hq", "gascity"}
+
+	plan := planDoltDrops(all, defaultStaleDatabasePrefixes, protected)
+
+	wantDrop := []string{"fixdepkeys_0d16f874c5d7", "testdb_old"}
+	if !equalStringSlice(plan.ToDrop, wantDrop) {
+		t.Errorf("ToDrop = %v, want %v", plan.ToDrop, wantDrop)
+	}
+	wantProtected := []string{"hq", "gascity"}
+	if !equalStringSlice(plan.Protected, wantProtected) {
+		t.Errorf("Protected = %v, want %v", plan.Protected, wantProtected)
+	}
+	if len(plan.Skipped) != 0 {
+		t.Errorf("Skipped = %v, want empty", plan.Skipped)
+	}
+}
+
 func TestPlanDoltDrops_SkipsInvalidDropIdentifiers(t *testing.T) {
 	all := []string{
 		"testdb_valid_1",
